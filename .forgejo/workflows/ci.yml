on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: node-lts
    steps:
      - uses: https://github.com/laytan/setup-odin@v2
        with:
          token: ${{ secrets.GH_TOKEN }}
      - run: apt update && apt install -y glslang-tools clang
      - run: make

